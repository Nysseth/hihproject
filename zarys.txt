Logowanie/Rejestracja
(Wylogowanie dla zalogowanych, brak logowania dla zalogowanych)
Galeria zdjęć
(Upload[PNG,JPG i poniżej 1mb, tytuł, autor(automatyczne wpisywanie gdy zalogowany), publiczne/prywatne dla zalogowanych],
przekształcanie uploadowanego obrazka(oryginał, ze znakiem wodnym, miniaturka)
wyszukiwarka Ajax(po każdej literce asynchroniczne zapytanie),
zapamiętywanie zaznaczonych przez usera i wyświetlenie tylko tych na innej podstronie[wraz z możliwością odzapamiętania],
rozróżnianie użytkowników i pokazywanie prywatnych tylko autorowi)

Responsywność CSS
sessionStorage i localStorage
Pole do wpisania swojego imienia

Create element na button w headerze(sekcja wprowadzająca)

jQuery UI:
- Dialog na login i register
- Selectable galerii
- Sortowanie galerii


Button tell me more - jeśli JS włączony, to create element, jeśli wyłączony, to href do historii
